apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: rfm
spec:
  type: NodePort
  selector:
    app: dashboard
  ports:
    - port: 8501
      targetPort: 8501
      # 8501 is taken on this machine by the Swarm stack.
      nodePort: 30085
