# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environment
.venv

# Secrets — .env.example is committed as the template
.env

# Hydra run directories
outputs/
multirun/

# Local MLflow tracking (runs now go to DagsHub)
mlflow.db
mlruns/

# Notebook exports — the pipeline writes data/target_dataset.csv instead
target_dataset*.xlsx
target_dataset*.csv

# DVC-managed outputs are ignored by the .gitignore files DVC writes
# into data/ and models/. metrics.json is deliberately NOT ignored: it is
# small, and keeping it in git is what makes `dvc metrics diff` useful.

# Tracked by DVC (see "Online Retail.xlsx.dvc")
/Online Retail.xlsx
